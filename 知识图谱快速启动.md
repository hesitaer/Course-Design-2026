# 🚀 知识图谱功能快速启动指南

## ✅ 已完成的功能

### 后端部分
- [x] 创建 `KnowledgeGraphDTO` - 知识图谱数据传输对象
- [x] 创建 `IKnowledgeGraphService` - 服务接口
- [x] 创建 `KnowledgeGraphServiceImpl` - 服务实现（从MySQL构建图谱）
- [x] 创建 `KnowledgeGraphController` - REST API控制器
- [x] 扩展 `ProductMapper` - 添加获取所有文物的方法
- [x] 扩展 `ProductServiceImpl` - 实现findAllProducts方法
- [x] 更新 `ProductMapper.xml` - 添加SQL查询

### 前端部分
- [x] 创建 `KnowledgeGraph.vue` - 知识图谱可视化页面
- [x] 集成 ECharts 力导向图
- [x] 实现节点点击、悬停、拖拽、缩放交互
- [x] 添加搜索过滤功能
- [x] 添加路由配置 `/knowledge-graph`
- [x] 在导航栏添加入口

---

## 📋 启动步骤

### 第一步：安装ECharts依赖

**请在终端执行：**
```bash
cd "d:\work\软件工程\课设\code\Course-Design-2026\Overseas_cultural_relics_knowledge_service subsystem\myvue"
npm install echarts
```

### 第二步：启动后端服务

**请在另一个终端执行：**
```bash
cd "d:\work\软件工程\课设\code\Course-Design-2026\Overseas_cultural_relics_knowledge_service subsystem\museum"
mvn spring-boot:run
```

等待看到类似以下信息表示启动成功：
```
Started DemoApplication in X.XXX seconds
```

后端将运行在：`http://localhost:8085`

### 第三步：启动前端服务

**请在第三个终端执行：**
```bash
cd "d:\work\软件工程\课设\code\Course-Design-2026\Overseas_cultural_relics_knowledge_service subsystem\myvue"
npm run dev
```

前端将运行在：`http://localhost:5173`（或其他端口，查看终端输出）

### 第四步：访问知识图谱

在浏览器中打开：
```
http://localhost:5173/#/knowledge-graph
```

或者点击导航栏的 **"知识图谱"** 菜单项。

---

## 🎯 功能演示

### 1. 基本浏览
- 页面加载后自动显示文物知识图谱
- 不同颜色代表不同类型的节点（文物、作者、朝代等）
- 节点大小根据类型自动调整

### 2. 交互操作
- **拖拽节点**：鼠标按住节点可拖动
- **缩放画布**：鼠标滚轮缩放
- **平移画布**：按住空白处拖动
- **查看详情**：点击节点弹出详细信息
- **悬停提示**：鼠标悬停显示节点属性

### 3. 搜索功能
- 在顶部搜索框输入关键词（如"瓷器"、"明代"等）
- 点击"搜索"按钮或按回车键
- 图谱将只显示匹配的节点和关系

### 4. 重置与刷新
- **重置**：清空搜索条件，显示全部数据
- **刷新**：重新从服务器获取数据

---

## 🔍 验证清单

启动后请检查以下内容：

- [ ] 后端控制台无错误日志
- [ ] 前端控制台无错误信息
- [ ] Network标签中看到成功的API请求（状态码200）
- [ ] 页面显示力导向图，有节点和连线
- [ ] 左上角显示图例（文物、作者、朝代等）
- [ ] 右上角显示节点数和关系数统计
- [ ] 可以拖拽节点
- [ ] 可以缩放和平移
- [ ] 点击节点弹出详情对话框
- [ ] 搜索功能正常工作

---

## 🐛 故障排查

### 问题1：前端报错 "echarts not found"
**解决方案**：确保已执行 `npm install echarts`

### 问题2：后端连接数据库失败
**解决方案**：
- 检查网络连接
- 确认 `application.yml` 中的数据库配置正确
- 联系数据库管理员确认远程服务器可用

### 问题3：图谱显示为空
**解决方案**：
- 检查后端是否返回数据（查看Network标签）
- 确认MySQL数据库中有文物数据
- 查看后端控制台是否有异常

### 问题4：跨域错误（CORS）
**解决方案**：如需配置CORS，请在后端添加配置类

---

## 📊 API测试

可以使用Postman或curl测试API：

```bash
curl -X POST http://localhost:8085/knowledge/graph \
  -H "Content-Type: application/json" \
  -d '{"keyword": ""}'
```

预期返回包含nodes和edges的JSON数据。

---

## 💡 下一步优化建议

1. **性能优化**：如果节点过多（>500），考虑分页或虚拟滚动
2. **Neo4j集成**：启用Neo4j查询展示更复杂的关系
3. **导出功能**：添加导出为图片功能
4. **全屏模式**：添加全屏查看按钮
5. **布局切换**：提供圆形、树形等布局选项

---

## 📞 需要帮助？

如果遇到任何问题：
1. 查看浏览器控制台的错误信息
2. 查看后端控制台的日志
3. 检查Network标签的API请求和响应
4. 参考 `知识图谱可视化功能说明.md` 文档

---

**祝使用愉快！** 🎉
