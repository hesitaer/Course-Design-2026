<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.FindImageMapper">
    <select id="findImage" resultType="String">
        select url from img_url_table where img_id = #{str}
    </select>
</mapper>