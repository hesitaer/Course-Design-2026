# 环境

前后端分离，前端使用Vue3，后端使用Spring Boot

前端页面 <http://localhost:8080>   后端 API <http://localhost:8085>
之后需要服务器部署再说。

## 基础环境

```md
Java 11 JDK 11
Maven 3.6+ 
Node.js ≥ 18.0.0 (我用的22.22)
npm ≥ 9.0.0(我用的10.9.7)
```

### 后端依赖(Maven 自动下载)

依赖已在 `pom.xml`中定义

### 前端依赖（npm 安装）

前端使用 Vue 3，依赖已在`package.json`

实在配不好，下个trae CN里面的ai还行，别用豆。也不要太相信ai，还是要自己看一看的。

数据库测试可以连接，但是图片爬得比较慢，所以还需要等等图数据库。
