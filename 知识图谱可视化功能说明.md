# 知识图谱可视化功能说明

## 📊 功能概述

本模块实现了海外文物知识图谱的可视化展示，采用力导向图展示文物实体及其关联关系，支持以下交互功能：

### 核心功能
- ✅ **力导向图展示**：动态展示文物、作者、朝代、博物馆、材质、文化等实体节点
- ✅ **节点点击展开**：点击节点显示详细信息弹窗
- ✅ **悬停显示属性**：鼠标悬停显示节点属性和关系
- ✅ **缩放与拖拽**：支持画布缩放、平移和节点拖拽
- ✅ **搜索过滤**：支持按关键词搜索特定文物或相关实体
- ✅ **分类着色**：不同类型节点使用不同颜色区分
- ✅ **关系标注**：边显示关系类型（作者、朝代、收藏于等）

## 🎨 节点类型与颜色

| 节点类型 | 颜色 | 大小 | 说明 |
|---------|------|------|------|
| 文物 (Relic) | 🔴 #FF6B6B | 60px | 文物实体，最大节点 |
| 作者 (Artist) | 🔵 #4ECDC4 | 45px | 文物创作者 |
| 朝代 (Dynasty) | 🔵 #45B7D1 | 40px | 文物所属朝代 |
| 博物馆 (Museum) | 🟠 #FFA07A | 35px | 收藏博物馆 |
| 材质 (Material) | 🟢 #98D8C8 | 30px | 文物材质 |
| 文化 (Culture) | 🟡 #F7DC6F | 30px | 文化/地域标签 |

## 🚀 使用方法

### 1. 启动后端服务

```bash
cd "Overseas_cultural_relics_knowledge_service subsystem/museum"
mvn spring-boot:run
```

后端服务将运行在 `http://localhost:8085`

### 2. 安装前端依赖并启动

```bash
cd "Overseas_cultural_relics_knowledge_service subsystem/myvue"
npm install echarts
npm run dev
```

前端服务通常运行在 `http://localhost:5173`

### 3. 访问知识图谱页面

在浏览器中访问：
```
http://localhost:5173/#/knowledge-graph
```

或在应用导航菜单中添加链接到 `/knowledge-graph`

## 📡 API接口

### 获取知识图谱数据

**接口地址**: `POST http://localhost:8085/knowledge/graph`

**请求参数**:
```json
{
  "keyword": "瓷器"  // 可选，搜索关键词
}
```

**响应格式**:
```json
{
  "state": 200,
  "message": "获取知识图谱成功",
  "data": {
    "nodes": [
      {
        "id": "relic_1_ABC123",
        "name": "青花瓷瓶",
        "category": "relic",
        "value": "明代",
        "symbolSize": 60,
        "color": "#FF6B6B"
      }
    ],
    "edges": [
      {
        "source": "relic_1_ABC123",
        "target": "artist_张三",
        "relationType": "作者",
        "label": "作者"
      }
    ]
  }
}
```

### 获取指定文物关联图谱

**接口地址**: `POST http://localhost:8085/knowledge/relic-graph`

**请求参数**:
```json
{
  "relicName": "青花瓷瓶"
}
```

## 🛠️ 技术栈

### 后端
- **Spring Boot 2.6.6**: Web框架
- **MyBatis**: 数据持久层
- **MySQL**: 关系型数据库（存储文物数据）
- **Neo4j**: 图数据库（预留，可扩展复杂关系查询）

### 前端
- **Vue.js 3.4.21**: 前端框架
- **ECharts 5.x**: 图表库（力导向图）
- **Element Plus**: UI组件库
- **Axios**: HTTP客户端

## 📁 文件结构

```
后端 (museum):
├── src/main/java/com/
│   ├── controller/
│   │   └── KnowledgeGraphController.java      # 知识图谱控制器
│   ├── entity/
│   │   └── KnowledgeGraphDTO.java             # 图谱数据传输对象
│   ├── service/
│   │   ├── IKnowledgeGraphService.java        # 服务接口
│   │   └── Impl/
│   │       └── KnowledgeGraphServiceImpl.java # 服务实现
│   ├── repository/
│   │   └── ProductRepository.java             # Neo4j仓库（预留）
│   └── mapper/
│       └── ProductMapper.java                 # MyBatis Mapper

前端 (myvue):
├── src/views/knowledge/
│   └── KnowledgeGraph.vue                     # 知识图谱页面
└── src/router/
    └── index.js                               # 路由配置（已添加）
```

## 💡 功能扩展建议

### 短期优化
1. **性能优化**：对大量节点进行分页加载或懒加载
2. **布局算法**：提供更多布局选项（圆形、树形等）
3. **导出功能**：支持导出图谱为图片（PNG/SVG）
4. **全屏模式**：添加全屏查看功能

### 中期扩展
1. **Neo4j集成**：启用Neo4j查询，展示更复杂的关联关系
2. **路径分析**：显示两个文物之间的关联路径
3. **时间轴**：按朝代时间轴展示文物演变
4. **统计面板**：展示各类实体的统计数据

### 长期规划
1. **AI推荐**：基于图谱的智能推荐系统
2. **3D可视化**：使用Three.js实现3D知识图谱
3. **协作标注**：多用户协同标注和补充关系
4. **AR/VR展示**：虚拟现实环境下的文物展示

## ⚠️ 注意事项

1. **数据库连接**：确保MySQL数据库可访问（配置在application.yml）
2. **跨域问题**：前后端分离需要配置CORS（如需要）
3. **数据量控制**：当前限制最多返回200个文物，避免浏览器性能问题
4. **网络延迟**：首次加载可能需要几秒，已添加loading状态

## 🐛 常见问题

### Q1: 图谱显示为空？
- 检查后端服务是否正常运行
- 检查MySQL数据库是否有数据
- 查看浏览器控制台是否有错误信息

### Q2: 节点重叠严重？
- 调整力导向图的排斥力参数（repulsion）
- 减少显示的节点数量

### Q3: 搜索无结果？
- 确认关键词拼写正确
- 尝试使用更通用的关键词

## 📞 技术支持

如有问题，请检查：
1. 后端日志：查看控制台输出
2. 前端控制台：查看Network和Console标签
3. 数据库连接：确认配置正确

---

**开发日期**: 2026-05-25  
**版本**: v1.0.0  
**作者**: 软件工程课设组2
